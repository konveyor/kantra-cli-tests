name: Nightly CLI test for main

on:
  schedule:
    - cron: '0 1 * * *'

jobs:
  test-suite:
    uses: konveyor/kantra-cli-tests/.github/workflows/test-suite.yaml@main
    secrets: inherit
    with:
      tag: latest
      tier: ALL
